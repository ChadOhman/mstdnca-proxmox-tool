Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
flask-sock==0.7.0
SQLAlchemy==2.0.52
proxmoxer==2.3.0
requests==2.34.2
# paramiko 5.0.0 fixes CVE-2026-44405 (flagged by pip-audit against 4.0.0) and the
# earlier Dependabot GHSA (low) rsakey.py SHA-1 advisory. Stable APIs used by
# clients/ssh_client.py (SSHClient, *Key.from_private_key, AutoAddPolicy) are unchanged.
paramiko==5.0.0
# cryptography 48.0.1 fixes GHSA-537c-gmf6-5ccf (flagged by pip-audit against
# 46.0.7). paramiko 5.0.0 and PyJWT[crypto] 2.13.0 both accept cryptography 48.x.
cryptography==50.0.0
APScheduler==3.11.3
Werkzeug==3.1.8
PyJWT[crypto]==2.13.0
gunicorn==26.2.0
# gevent is the gunicorn worker class (see scripts/setup.sh ExecStart) and is
# imported directly by seven route modules (routes/elk.py, ghost.py, jibri.py,
# jitsi.py, mastodon.py, peertube.py, prometheus_app.py) to spawn background
# greenlets for long-running SSH/upgrade work. It must ship in requirements.txt
# (not installed ad-hoc) so a venv built from this file starts cleanly and the
# dependency is visible to pip-audit/Dependabot.
gevent==26.8.0
geoip2==5.3.0
prometheus-client==0.26.0
# Claude API client for the optional in-app AI assistant (routes/ai_chat.py).
anthropic==0.85.0
